export default {
  // canvas target node
  // where every canvas will be appended to
  // @type {string} selector
  targetNode: 'body',
  // canvas width and height
  // this has to be set from the renderer
  canvasSize: [0, 0],
  // tile size
  // general size of every tile
  // single value as tile is always a square
  tileSize: 50,
  // tile size should be removed
  // block size is better name
  blockSize: 64,
};
